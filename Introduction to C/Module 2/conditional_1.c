#include<stdio.h>
int main()
{
    int tk;
    scanf("%d", &tk);
    if (tk >= 100)
    {
        printf("I will eat the bruger");
    }
    else
    {
        printf("I wont't eat the bruger");
    }
    return 0;
}